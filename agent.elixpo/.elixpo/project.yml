project:
  name: agent.elixpo
  description: "Open Source Agentic CI System"
  stack: python
  node_version: "20"

ci:
  lint_cmd: "cd core && ruff check ."
  test_cmd: "cd core && pytest -v"
  build_cmd: ""

maintainers:
  - github: Circuit-Overtime
    skills: [python, ci, devops, github-actions, llm, fastapi]
    lead: true

gist:
  id: ""
  account: elixpoo
