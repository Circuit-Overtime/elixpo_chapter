name: Publish @elixpo/lixblogs-cli

on:
  push:
    tags:
      - 'lixblogs-cli-v*'
  workflow_dispatch:
    inputs:
      publish:
        description: Publish the current package after the release gate
        required: true
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  gate:
    uses: ./.github/workflows/lixblogs-cli-release-gate.yml

  publish:
    name: Publish signed package
    needs: gate
    if: github.ref_type == 'tag' || inputs.publish == true
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/download-artifact@v4
        with:
          name: lixblogs-cli-package
          path: dist
      - name: Verify tag and package versions
        if: github.ref_type == 'tag'
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/lixblogs-cli/package.json').version")
          test "$GITHUB_REF_NAME" = "lixblogs-cli-v${PACKAGE_VERSION}"
      - name: Attest package provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/*.tgz
      - name: Publish to npm with provenance
        run: npm publish dist/*.tgz --access public --provenance
      - name: Publish generated changelog and artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "lixblogs-cli-v$(node -p "require('./packages/lixblogs-cli/package.json').version")"
          dist/*.tgz
          --generate-notes
          --title "@elixpo/lixblogs-cli $(node -p "require('./packages/lixblogs-cli/package.json').version")"
